# 07. Дизайн-система

Основана на макете «Clean Office». Задача документа — зафиксировать решения,
чтобы 11 посадочных страниц, блог и кейсы выглядели как один сайт, а не как
одиннадцать разных.

---

## 1. Визуальная идея

Клининг продаёт не процесс, а **состояние после**: свет, воздух, отсутствие
беспорядка. Отсюда направление, заданное макетом, и его стоит сохранить:
холодная сине-голубая палитра, много белого пространства, карточки с мягкими
тенями, крупные светлые фотографии.

Ошибка, которой нужно избежать: перегрузить страницу градиентами и стеклянными
эффектами. Сине-голубой градиент на каждом втором блоке — самый частый штамп
в этой тематике. В макете градиентных секций уже четыре подряд — стоит
чередовать их с белыми, чтобы глаз отдыхал и структура читалась.

**Один акцент на страницу.** Если всё выделено, не выделено ничего.

---

## 2. Цветовые токены

```css
:root {
  /* Основа бренда */
  --brand-900: #0A2540;  /* глубокий синий — тёмные секции, футер */
  --brand-700: #14487F;  /* основной синий — заголовки, шапка */
  --brand-500: #1E7BC8;  /* рабочий синий — ссылки, границы активных элементов */
  --brand-300: #4FA9E8;  /* голубой акцент — подсветка слов в заголовках */
  --brand-100: #E8F3FC;  /* очень светлый голубой — фон блоков, иконки */

  /* Акцент действия — единственный тёплый цвет на сайте */
  --accent-600: #E8720C; /* кнопки CTA, важные подсветки */
  --accent-500: #FF8A1F;
  --accent-100: #FFF0E0;

  /* Нейтральные */
  --ink-900: #0F1A28;    /* основной текст */
  --ink-600: #4A5A6C;    /* вторичный текст */
  --ink-400: #8A98A8;    /* подписи, плейсхолдеры */
  --line:    #DCE5EE;    /* границы, разделители */
  --bg:      #FFFFFF;
  --bg-soft: #F5F8FB;    /* фон чередующихся секций */

  /* Статусы */
  --ok:    #1B9A5A;
  --warn:  #D9A400;
  --error: #D93B3B;
}
```

**Про акцентный оранжевый.** В макете все кнопки синие — того же оттенка, что
и фон. Из-за этого призывы к действию теряются. Один тёплый акцент на весь
сайт, использованный только для целевых кнопок, поднимает заметность CTA
без изменения общего впечатления. Это единственное отступление от макета,
которое рекомендуется, — и его стоит проверить A/B-тестом.

**Контраст:** любой текст на цветном фоне проверять на соотношение не ниже
4.5:1. Белый текст на `--brand-500` проходит; белый на `--brand-300` — нет,
только для крупных заголовков от 24 px.

---

## 3. Типографика

```css
--font-display: "Manrope", "Segoe UI", system-ui, sans-serif;  /* заголовки */
--font-body:    "Inter", system-ui, -apple-system, sans-serif;  /* текст */
```

Manrope выбран вместо привычного Montserrat: у него более узкие прописные и
характерная геометрия, заголовки набираются плотнее и выглядят собраннее.
Оба шрифта имеют полную кириллицу и свободную лицензию.

### Шкала

| Роль | Десктоп | Мобильный | Начертание | Межстрочный |
|------|---------|-----------|-----------|-------------|
| H1 | 52 px | 30 px | 800 | 1.1 |
| H2 (секции) | 38 px | 25 px | 800 | 1.15 |
| H3 | 26 px | 21 px | 700 | 1.25 |
| H4 / заголовок карточки | 20 px | 18 px | 700 | 1.3 |
| Основной текст | 17 px | 16 px | 400 | 1.6 |
| Крупный лид | 20 px | 17 px | 400 | 1.55 |
| Подпись, сноска | 14 px | 13 px | 400 | 1.45 |
| Кнопка | 16 px | 16 px | 700 | 1 |
| Надзаголовок (eyebrow) | 13 px | 12 px | 700, letter-spacing .08em, uppercase | 1.2 |

### Правила набора

- Заголовки секций — **прописными не полностью**. В текущем макете H2 набраны
  капсом целиком (`ПОЧЕМУ МЫ?`, `ЭТАПЫ ОБРАЩЕНИЯ`), это снижает скорость
  чтения примерно на 10–15 %. Оставить капс только для короткого надзаголовка
  над H2, сам заголовок — обычным регистром с жирным начертанием.
- Приём из макета — **подсветка части заголовка цветом** («УБОРКА *ОФИСНЫХ
  ЗДАНИЙ*») — рабочий, сохранить. Подсвечивается только значимое слово, не
  половина фразы.
- Длина строки основного текста — 60–75 символов. Абзацы в описательных блоках
  макета сейчас длиннее и набраны мелко; сократить.
- Никогда не набирать основной текст светло-серым на белом (`--ink-400`) —
  только `--ink-900` или `--ink-600`.
- Цифры в блоке доверия и в кейсах — крупно, `font-variant-numeric: tabular-nums`.

---

## 4. Сетка и отступы

```css
--container: 1200px;
--gutter-desktop: 32px;
--gutter-mobile: 16px;

/* Шкала отступов, шаг 4 */
--sp-1: 4px;   --sp-2: 8px;   --sp-3: 12px;  --sp-4: 16px;
--sp-5: 24px;  --sp-6: 32px;  --sp-7: 48px;  --sp-8: 64px;
--sp-9: 96px;  --sp-10: 128px;
```

- Колонок: 12 на десктопе, 6 на планшете, 4 на мобильном.
- Вертикальный ритм секций: 96 px десктоп / 56 px мобильный. Одинаково для
  всех секций — разнобой в отступах главная причина ощущения «сайт собран из кусков».
- Секции чередуются по фону: белая → `--bg-soft` → белая → тёмная (акцентная).
  Подряд не более двух цветных секций.

---

## 5. Компоненты

### Кнопки

| Тип | Применение | Стиль |
|-----|-----------|-------|
| Primary | Целевое действие, одна на экран | Фон `--accent-600`, белый текст, радиус 8 px, высота 52 px, паддинг 24 px |
| Secondary | Второе действие рядом | Прозрачный фон, рамка 2 px `--brand-500`, текст `--brand-700` |
| Ghost | Третьестепенные | Только текст `--brand-500`, подчёркивание при наведении |
| Link-arrow | «Подробнее →» в карточках | Текст + стрелка, стрелка смещается вправо при наведении |

Состояния: `hover` (затемнение на 8 %), `active` (сдвиг на 1 px вниз),
`focus-visible` (обводка 3 px `--brand-300` с отступом 2 px — **обязательно**,
без неё сайт непроходим с клавиатуры), `disabled` (непрозрачность 0.5),
`loading` (спиннер, текст «Отправляем…»).

Минимальная зона касания на мобильном — 44×44 px.

### Карточки

```css
--radius-sm: 8px;
--radius-md: 12px;
--radius-lg: 20px;
--shadow-sm: 0 1px 3px rgba(15,26,40,.08);
--shadow-md: 0 4px 16px rgba(15,26,40,.10);
--shadow-lg: 0 12px 32px rgba(15,26,40,.14);
```

Карточка сегмента: белый фон, `--radius-md`, `--shadow-sm`, при наведении —
`--shadow-md` и подъём на 2 px, переход 180 мс. Внутри: иконка 48 px в
кружке `--brand-100`, заголовок H4, строка описания, ссылка-стрелка.

### Иконки

Единый набор: линейные, толщина обводки 1.5–2 px, размер 24/32/48 px,
цвет `--brand-500`. Не смешивать линейные и заливные иконки в одном блоке.
В текущем сайте иконки разнородные (`slide2-1.svg`…`slide4-4.svg`) —
перерисовать одним набором.

### Формы

Поле: высота 52 px, рамка 1 px `--line`, радиус 8 px, при фокусе рамка
`--brand-500` + тень. Label — над полем, не плейсхолдером (плейсхолдер
исчезает при вводе, пользователь забывает, что вводит).
Ошибка — красная рамка + текст под полем, не всплывающая подсказка.

### Слайдер «до/после»

Шторка с перетаскиванием: вертикальный разделитель, круглая ручка 40 px,
работает мышью, касанием и стрелками клавиатуры. Подпись под слайдером:
тип объекта, площадь, вид работ.

---

## 6. Фотографии

**Обязательно:**
- реальные фото объектов и команды в блоках «Кейсы», «До/после», «Команда»;
- люди в фирменной одежде, узнаваемой на всех фото;
- светлые кадры с естественным освещением, холодная цветокоррекция
  в тон палитре;
- горизонтальный формат 3:2 для кейсов, 4:3 для «до/после» (пары одинаковых
  ракурсов — иначе сравнение не читается).

**Недопустимо:**
- сток в блоках «Команда», «Кейсы», «Отзывы» — его узнают, и весь блок
  начинает работать против доверия;
- фотографии с логотипами клиентов без письменного разрешения;
- разные ракурсы в паре «до/после».

Сток допустим только в декоративных фонах первого экрана и конверсионных
блоков — как в макете.

---

## 7. Движение

Сдержанно. Анимация в B2B-сайте должна помогать понимать интерфейс,
а не развлекать.

- Появление секций при скролле: смещение 16 px + прозрачность, 400 мс,
  один раз, только для блоков с карточками.
- Ховеры: 150–200 мс.
- Счётчики цифр в блоке доверия: анимация от нуля при появлении — один раз.
- Аккордеон FAQ: плавное раскрытие 250 мс.
- Обязательно: `@media (prefers-reduced-motion: reduce) { * { animation: none !important; transition: none !important; } }`

Чего не делать: параллакс, автопрокрутка каруселей, анимированные градиенты
на фоне, появление элементов с задержкой более 400 мс.

---

## 8. Что скорректировать относительно макета

| Что в макете | Проблема | Решение |
|--------------|----------|---------|
| Все кнопки синие на синем | CTA теряются | Оранжевый акцент только на целевых кнопках |
| 4 градиентные секции подряд | Тяжело, теряется структура | Чередовать с белыми |
| H2 полностью капсом | Медленнее читается | Капс только для надзаголовка |
| Мелкий кегль в описаниях | Не читают | 17 px, абзацы короче |
| 3 логотипа клиентов | Читается как «клиентов мало» | 8–12 или убрать блок |
| Нет мобильных экранов | Больше половины трафика | Отрисовать все блоки для 360–430 px |
| Бедный футер | Теряется навигация и доверие | Полная карта + реквизиты |
| Нет состояний компонентов | Разработчик додумает по-своему | Отрисовать hover / focus / error / loading / пустое состояние |

---

## 9. Что передать разработчику

- [ ] Макеты: 1440 / 768 / 390 px для **всех** страниц, не только главной
- [ ] Шаблон посадочной страницы услуги
- [ ] Шаблон кейса и страницы блога
- [ ] Все состояния компонентов
- [ ] Экраны калькулятора, включая пошаговый мобильный режим
- [ ] Состояния форм: успех, ошибка, отправка
- [ ] Страница 404
- [ ] Иконки в SVG одним набором
- [ ] Файл токенов (цвета, отступы, типографика)
- [ ] Логотип в SVG: полная и сокращённая версии, светлая и тёмная
